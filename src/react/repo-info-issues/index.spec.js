import { expect } from 'chai';

import RepoInfoWithData from './index';

describe('#RepoInfoWithData()', () => {
  it('should throw an error if ownerName not provided', () => {
    expect(true).to.be.true;
  });

  it('should throw an error if name not provided', () => {
    expect(true).to.be.true;
  });

  it('should render stars counter', () => {
    expect(true).to.be.true;
  });

  it('should render forks counter', () => {
    expect(true).to.be.true;
  });

  it('should render last updated', () => {
    expect(true).to.be.true;
  });

  it('should render issues list', () => {
    expect(true).to.be.true;
  });
});